{
  "name": "back-end",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "jest --watchAll",
    "start": "SET \"NODE_ENV=production\" || export NODE_ENV=production || NODE_ENV=production && node index.js",
    "dev": "SET \"NODE_ENV=development\" || export NODE_ENV=development || NODE_ENV=development && node index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^3.0.4",
    "concurrently": "^4.1.0",
    "cors": "^2.8.5",
    "express": "^4.16.4",
    "jest": "^24.1.0",
    "mongoose": "^5.4.11",
    "nodemon": "^1.18.10"
  }
}
